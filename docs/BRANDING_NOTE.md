The icon shown in Home Assistant integration cards is driven by the HA brands system for the integration domain.
Local panel/sidebar logos are bundled in:
custom_components/padspan_ha/www/padspan-ha/assets/

If the card icon still does not show your logo, it requires a brands entry for domain `padspan_ha`.
