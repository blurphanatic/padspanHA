{
  "config": {
    "step": {
      "user": {
        "title": "PadSpan HA",
        "description": "Configure PadSpan settings.",
        "data": {
          "demo_mode": "Demo mode",
          "api_base": "API base URL",
          "api_key": "API key",
          "enable_sidebar": "Enable sidebar panel"
        }
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "PadSpan HA options",
        "data": {
          "refresh_seconds": "Refresh interval (seconds)",
          "enable_sidebar": "Enable sidebar panel"
        }
      }
    }
  }
}
