{
  "config": {
    "step": {
      "user": {
        "title": "Set up PadSpan HA",
        "description": "Cloud/API is optional. You can run local-only.",
        "data": {
          "enable_cloud": "Enable cloud hub connection",
          "hub_url": "Hub URL (optional)",
          "api_key": "Cloud API key (optional)",
          "scan_interval": "Update interval (seconds)"
        }
      }
    },
    "abort": {
      "already_configured": "PadSpan HA is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "PadSpan HA options",
        "data": {
          "enable_cloud": "Enable cloud hub connection",
          "hub_url": "Hub URL (optional)",
          "api_key": "Cloud API key (optional)",
          "scan_interval": "Update interval (seconds)"
        }
      }
    }
  }
}
