{
  "title": "PadSpan HA",
  "config": {
    "step": {
      "user": {
        "title": "Set up PadSpan HA",
        "description": "Configure BLE ingest and map defaults.",
        "data": {
          "name": "Name",
          "map_id": "Default map id",
          "include_passive": "Include passive/non-connectable BLE",
          "bootstrap_cache": "Bootstrap from discovered BLE cache",
          "device_timeout": "Device timeout (seconds)",
          "hub_sources_csv": "Hub source filter (comma-separated, optional)"
        }
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "PadSpan HA options",
        "description": "Adjust BLE options and optionally import a map image.",
        "data": {
          "name": "Name",
          "map_id": "Default map id",
          "include_passive": "Include passive/non-connectable BLE",
          "bootstrap_cache": "Bootstrap from discovered BLE cache",
          "device_timeout": "Device timeout (seconds)",
          "hub_sources_csv": "Hub source filter (comma-separated, optional)",
          "map_id_import": "Map id to import (optional)",
          "map_source_path": "Map source path under /config (optional)",
          "overwrite_map": "Overwrite existing map file"
        }
      }
    },
    "error": {
      "map_import_failed": "Map import failed. Check path and permissions."
    }
  }
}