{
  "title": "PadSpan HA",
  "config": {
    "step": {
      "user": {
        "title": "Set up PadSpan HA",
        "description": "Configure BLE behavior and update cadence.",
        "data": {
          "include_passive": "Include passive/non-connectable BLE",
          "update_interval": "Update interval (seconds)",
          "seen_timeout": "Presence timeout (seconds)",
          "enable_sidebar": "Enable advanced sidebar panel"
        }
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "PadSpan HA options",
        "data": {
          "include_passive": "Include passive/non-connectable BLE",
          "update_interval": "Update interval (seconds)",
          "seen_timeout": "Presence timeout (seconds)",
          "enable_sidebar": "Enable advanced sidebar panel"
        }
      }
    }
  }
}
