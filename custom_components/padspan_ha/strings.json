{
  "config": {
    "step": {
      "user": {
        "title": "Set up PadSpan",
        "description": "Cloud/API is optional. Local-only mode starts immediately.",
        "data": {
          "enable_cloud": "Enable cloud hub connection",
          "hub_url": "Hub URL (optional)",
          "api_key": "Cloud API key (optional)",
          "scan_interval": "Update interval (seconds)"
        }
      }
    },
    "abort": {
      "already_configured": "PadSpan is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "PadSpan options",
        "data": {
          "enable_cloud": "Enable cloud hub connection",
          "hub_url": "Hub URL (optional)",
          "api_key": "Cloud API key (optional)",
          "scan_interval": "Update interval (seconds)"
        }
      }
    }
  }
}
