import_map_image:
  name: Import map image
  description: Import an image from /config path into PadSpan local map storage.
  fields:
    entry_id:
      required: false
      selector:
        text:
    map_id:
      required: true
      selector:
        text:
    source_path:
      required: true
      selector:
        text:
    name:
      required: false
      selector:
        text:
    activate:
      required: false
      default: true
      selector:
        boolean:

set_map_anchor:
  name: Set map anchor
  description: Set or update scanner anchor coordinates on a map.
  fields:
    entry_id:
      required: false
      selector:
        text:
    map_id:
      required: true
      selector:
        text:
    source_id:
      required: true
      selector:
        text:
    x:
      required: true
      selector:
        number:
    y:
      required: true
      selector:
        number:
    z:
      required: false
      default: 0
      selector:
        number:
    weight:
      required: false
      default: 1
      selector:
        number:
    label:
      required: false
      selector:
        text:

delete_map_anchor:
  name: Delete map anchor
  description: Delete a scanner anchor from a map.
  fields:
    entry_id:
      required: false
      selector:
        text:
    map_id:
      required: true
      selector:
        text:
    source_id:
      required: true
      selector:
        text:

set_active_map:
  name: Set active map
  description: Set active map used for positioning.
  fields:
    entry_id:
      required: false
      selector:
        text:
    map_id:
      required: true
      selector:
        text:

reload_ble_cache:
  name: Reload BLE cache
  description: Force immediate BLE refresh.
  fields:
    entry_id:
      required: false
      selector:
        text:
