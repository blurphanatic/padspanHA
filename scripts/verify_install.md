# Verify Install

1) Restart Home Assistant after copying the integration.

2) Open **PadSpan HA** panel → Diagnostics.

3) Confirm:
- UI stamp: v0.4.0 • build <ID>
- Backend stamp: version/build_id in JSON

If the versions differ:
- You have duplicate installs (HACS + manual) or browser cache.
